{
  "title": "Jira",
  "description": "Connector for handling Jira issues",
  "credentials": {
    "fields": {
        "baseUrl": {
            "label": "Base URL for your Jira instance:",
            "viewClass": "TextFieldView",
            "required": true
        },
        "apiKey": {
            "label": "API Key",
            "viewClass": "TextFieldView",
            "required": true
        }
    }
  },
  "triggers": { 
      "getNewIssues": {
          "main": ".lib/triggers/getNewIssues.js",
          "type": "polling",
          "title": "Get New Issues",
          "fields": {
            "projects": {
                "label": "Project codes:",
                "required": "true",
                "viewClass": "TextFieldView",
                "note": "Please enter comma separated project codes",
                "model": "*"
            },
            "startDateTime": {
                "label": "Issues created since:",
                "required": true,
                "viewClass": "TextFieldView",
                "placeholder": "yyyy-mm-dd hh:mm",
                "note": "Please use <code><b>yyyy-mm-dd hh:mm</b></code> format and use the same timezone as the one configured in JIRA.",
                "model": "*"
            },
              "labels": {
                  "label": "Labels used to determine issues that should be retrieved:",
                  "required": "true",
                  "viewClass": "TextFieldView",
                  "note": "Please enter comma separated labels",
                  "model": "*"
              },
              "exclLabels": {
                  "label": "Labels used to determine issues that should be excluded:",
                  "required": "true",
                  "viewClass": "TextFieldView",
                  "note": "Please enter comma separated excluded labels",
                  "model": "*"
              }
          },
          "metadata": {
              "out": "./lib/schemas/issue.out.json"
          }
      }
  },
  "actions": { 
      "createNewIssues": {
          "main": ".lib/actions/createNewIssues.js",
          "title": "Create new issues",
          "fields": {
              "id": {
                "label": "Text ID",
                "required": true,
                "viewClass": "TextFieldView"
              }
          },
          "metadata": {
              "in": ".lib/schemas/issue.in.json"
          }
      }
  }
}
